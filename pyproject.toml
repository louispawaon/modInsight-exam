[project]
name = "receipt-intel"
version = "0.1.0"
description = "Ollama-first receipt intelligence challenge scaffold."
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

